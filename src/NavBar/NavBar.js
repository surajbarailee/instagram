const navWrapper={
    color:'red',
    borderBottom:'1px solid black',
}

const NavBar=()=>{
    return (
    <div className={navWrapper}>
            NavBar
    </div>
    )
}
export default NavBar;